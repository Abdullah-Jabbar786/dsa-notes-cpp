#include "iostream"
using namespace std;

// Array representation of a Heap
/*
    if a node is at index = i
    parent = i/2
    left child  = 2*i
    right child = 2*i + 1
*/
// If there are missing nodes leave a blank there.
